# Frontend

Angular 18 requires Node 18.19+ (Node 20 LTS is recommended).

```bash
npm install
npm start
```

The development server runs at `http://localhost:4200` and proxies `/api` to
the FastAPI service on port 8000.

