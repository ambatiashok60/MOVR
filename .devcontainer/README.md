# Remote preview

Open the repository in GitHub Codespaces. Node 20 and Python 3.11 are configured and ports 4200
and 8000 are forwarded. Start the backend with the mock LLM and the frontend using the commands in
`ai-workspace/PREVIEW.md`; use host `0.0.0.0` inside Codespaces.
